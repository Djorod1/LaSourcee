# Dépendances Python de LaSourcee.
#
# Ce fichier est à la racine du dépôt parce que c'est là que Vercel le
# cherche pour installer l'environnement de la fonction serverless.
# backend/requirements.txt y renvoie : il n'y a qu'une seule liste à
# maintenir.

# --- Serveur web -----------------------------------------------------
Flask==3.0.3

# --- Sécurité --------------------------------------------------------
bcrypt==4.2.0

# --- Configuration ---------------------------------------------------
python-dotenv==1.0.1

# --- Authentification externe ---------------------------------------
google-auth==2.32.0
requests==2.32.3

# --- Bases de données ------------------------------------------------
# SQLite est fourni par la bibliothèque standard : rien à installer.
#
# PostgreSQL — obligatoire pour un hébergement sans disque persistant
# (Vercel, Render, Railway). La distribution « binary » embarque les
# bibliothèques natives, aucune compilation n'est nécessaire.
psycopg2-binary==2.9.9

# MySQL — utilisé uniquement si DB_TYPE=mysql.
PyMySQL==1.1.1
cryptography==43.0.1
